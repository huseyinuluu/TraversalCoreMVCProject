<div class="leave-comment-form mt-5" id="reply">
    <h4 class="side-title mb-2">Yorum Bırakın</h4>
    @*<p class="mb-4">
            E-mail adresiniz yayınlanmayacak.
        </p>*@
    <form method="post" asp-action="AddComment" asp-controller="Comment">
        <input type="hidden" name="DestinationID" value="@ViewBag.destID" />
        <input type="hidden" name="AppUserID" value="@ViewBag.userID" />

        <div class="form-group col-lg-6">
            <input type="hidden" name="CommentUser" class="form-control" value="@ViewBag.userName" />
        </div>

        <!--</div>-->

        <div class="form-group">
            <textarea name="CommentContent" class="form-control" placeholder="Mesajınız" required=""
                      spellcheck="false"></textarea>
        </div>

        <div class="submit text-right">
            <button class="btn btn-style btn-primary">Yorum Yap</button>
        </div>
    </form>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.min.js" type="text/javascript"></script>